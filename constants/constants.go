package constants

// Context keys
const (
	// Authentication
	AuthUserKey   = "auth.user"
	AuthAPIKeyKey = "auth.api_key"

	// task
	TaskKey = "task"
)

// Errors
const (
	ValidationFailed = "Validation Failed"
)
